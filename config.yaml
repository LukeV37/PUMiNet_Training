################################
### Configuration Parameters ###
################################
# epochs: 20
# epochs: 200
epochs: 100
# in_sample: "/home/rakib/Physics-Datasets/DenoisingAE/10k_Events/data_combined.pkl"
in_sample: "/home/rakib/Physics-Datasets/DenoisingAE/100k_Events/data_combined.pkl"
# out_path: "output/results-10k"
out_path: "output/results-100k"
# embed_dim: 16
embed_dim: 64 
num_heads: 4   # 64/4 = 16 is the dimension of each head
# latent_dim: 4
latent_dim: 16 # latent dim < embed_dim. 4 times compression
learning_rate: 0.0001
# lr_step_size: 12
lr_step_size: 50
lr_step_gamma: 0.1
# eval_model: "./output/results-10k/best_model_weights-embed64-head4-latent16.pth"
eval_model: "./output/results-100k/best_model_weights-embed64-head4-latent16.pth"